spring:
  config:
    import: optional:classpath:secrets_auth.yaml

  application:
    name: authorization-server

  security:
    oauth2:
      authorizationserver:
        issuer: http://192.168.1.102:9000

server:
  port: 9000