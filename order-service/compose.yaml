services:
  postgres:
    image: 'postgres:16'
    environment:
      POSTGRES_DB: order_service_db
      POSTGRES_PASSWORD: tibor
      POSTGRES_USER: titus
    ports:
      - '5432:5432'
    restart: always
    